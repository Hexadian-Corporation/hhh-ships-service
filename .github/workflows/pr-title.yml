name: PR Title

on:
  pull_request:
    types: [opened, reopened, edited, synchronize]

jobs:
  validate:
    name: Validate PR Title
    runs-on: ubuntu-latest
    steps:
      - name: Check title format
        uses: actions/github-script@v7
        with:
          script: |
            const title = context.payload.pull_request.title;
            const pattern = /^(SHIPS-\d+|chore|fix|ci|docs|feat|refactor|test|build|perf|style|revert): .+$/;
            if (!pattern.test(title)) {
              core.setFailed(
                `PR title "${title}" doesn't match the required format.\n` +
                `Expected: "SHIPS-N: description" or "<type>: description"\n` +
                `Allowed types: chore, fix, ci, docs, feat, refactor, test, build, perf, style, revert`
              );
            }
